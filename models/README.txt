Trained model weights are stored here after running:

    python src/train.py

Output file: ai_vs_real_classifier.h5

Model weights are excluded from git (.gitignore) due to file size.
To reproduce, run the full pipeline:
  1. python src/preprocess.py
  2. python src/train.py
